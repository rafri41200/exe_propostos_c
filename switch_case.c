#include <stdio.h>

int main()
{
  int N1,N2;
  unsigned escolha;

  punts("informe dois numeros");
  scanf("%d %d",&N1,&N2);
  punts("digite (1) para dividir esses valores,(2) para somar (3) para subtrair");
  scanf
  switch (escolha)
  {
  case :
    /* code */
    break;
  
  default:
    break;
  }
}